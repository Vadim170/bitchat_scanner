<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Android 11 and lower BLE compatibility permissions. -->
    <uses-permission
        android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />

    <!-- Android 12+ BLE scanning and device metadata.  BLUETOOTH_SCAN is
         declared without the usesPermissionFlags location opt-out because
         detections are combined with the phone position.  As a consequence the
         Bluetooth stack only delivers scan results while ACCESS_FINE_LOCATION
         is granted and Location Services are on, so precise location is a
         required permission on every supported version. -->
    <uses-permission
        android:name="android.permission.BLUETOOTH_SCAN"
        tools:targetApi="s" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- Optional, user-enabled detection notifications (Android 13+). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    
    <!-- Map tiles only; detection records never leave the device. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-feature
        android:name="android.hardware.bluetooth_le"
        android:required="true" />
    
    <!-- allowBackup=false disables cloud backup; dataExtractionRules also
         excludes the detection database from Android 12+ device-to-device
         transfer, which allowBackup alone does not prevent on every OEM. -->
    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BitchatScanner">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.BitchatScanner">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Receives only this app's explicit BluetoothLeScanner PendingIntent. -->
        <receiver
            android:name=".BleScanReceiver"
            android:exported="false" />

    </application>
</manifest>
